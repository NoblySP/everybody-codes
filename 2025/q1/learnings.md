# Swap `swap(x, y)`
x, y = y, x
This works in Python

# Modulo
- Use `x % len(list)` for modulo operations (when dealing with 0 indexed arrays)

- Python's modulo operator always returns a +ve integer. Explanation: https://stackoverflow.com/a/67139303
- This is in contrast to C's % operator

